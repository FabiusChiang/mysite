QUnit=require("QUnit");

QUnit.module("Mappers: toRiskRewards");

QUnit.test("able to form risk rewards correctly", function (assert) {
    assert.equal(1, 1, "1=1 must happen!");
    assert.equal(1, 2, "2=2 should happen!");
    ok(1 == "1");
});